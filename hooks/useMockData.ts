
import type { BkuData, BkpData, JuDisplayRow, BudgetItem } from '../types';

// --- BKU MOCK DATA ---
export const getInitialBkuData = (): BkuData[] => {
    return [];
};


// --- BKP MOCK DATA ---
export const getInitialBkpData = (): BkpData[] => {
    return [];
};

// --- JURNAL UMUM MOCK DATA ---
export const getInitialJuData = (): JuDisplayRow[] => {
  return [];
};

// --- BUDGET ITEM MOCK DATA ---
export const getInitialBudgetItems = (): BudgetItem[] => {
    return [];
};
